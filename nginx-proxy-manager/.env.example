# ── Nginx Proxy Manager Ports ─────────────────────────────────────────────
# Ports 80 and 443 must be free on the host for HTTP/HTTPS traffic and
# Let's Encrypt certificate issuance.
NPM_HTTP_PORT=80
NPM_HTTPS_PORT=443
NPM_ADMIN_PORT=81
